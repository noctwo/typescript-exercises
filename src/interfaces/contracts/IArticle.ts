// * wir legen auch für den Artikel ein Interface an, weil wir sicherstellen wollen,
// * dass es immer die beiden Eigenschaften number und quantity gibt
interface IArticle {
    number: number;
    quantity: number;
  }
  
  export default IArticle;